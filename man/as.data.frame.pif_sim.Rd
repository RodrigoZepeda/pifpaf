% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pifpaf_class.R
\name{as.data.frame.pif_sim}
\alias{as.data.frame.pif_sim}
\title{Transform to data.frame}
\usage{
\method{as.data.frame}{pif_sim}(x, ...)
}
\arguments{
\item{x}{A \code{pif_sim} object}

\item{...}{Additional methods to pass to \code{\link[=summary.pif_sim]{summary.pif_sim()}}}
}
\description{
Method for transforming a potential impact fraction or a
population attributable fraction to a data.frame
}
\examples{
#Example 1
options(survey.lonely.psu = "adjust")
design <- survey::svydesign(data = ensanut, ids = ~1, weights = ~weight, strata = ~strata)
rr <- function(X, theta) {
  exp(-2 +
    theta[1] * X[, "age"] + theta[2] * X[, "systolic_blood_pressure"] / 100)
}
cft <- function(X) {
  X[, "systolic_blood_pressure"] <- X[, "systolic_blood_pressure"] - 5
  return(X)
}
pifsim <- pif(design,
  theta = log(c(1.05, 1.38)), rr, cft,
  additional_theta_arguments = c(0.01, 0.03), n_bootstrap_samples = 10, parallel = FALSE
)
as.data.frame(pifsim)
}
